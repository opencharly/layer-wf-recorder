# 2026.245.1357 — fix(layer): add arch/omarchy package mapping for wf-recorder

## Summary

Gives `layer-wf-recorder` a real **arch/omarchy package mapping** (it only had `fedora`), so composing the layer on omarchy/arch guests actually installs wf-recorder — today it is a no-op there.

**Why (measured):** on the omarchy VM spike (C1) and the omarchy desktop-pod spike (C2), record: desktop failed at binary discovery (no desktop recorder available) and the layer own baked checks failed (wf-recorder binary installed = false) — the layer installed nothing on arch-family guests. Verified the package exists in Arch extra (against the omarchy frozen snapshot: pacman -Si wf-recorder -> Repository: extra, wf-recorder 0.6.0-2). This unblocks the VM and pod desktop-recording lanes on omarchy.

**Changes:** `distro:` adds `arch` + `omarchy` -> `package: [wf-recorder]`; the candy skill package line notes PAC too. The `require:` plugin-record pin is untouched (cross-repo lag is resolved by reconcile per the layer own comment; `record_env` support in plugin-record is landable independently).

## How tested

**Gate:** fresh-rebuild disposable omarchy VM composing the CHANGED layer (PR head — the arch/omarchy mapping, loaded into the layer snapshot at the pinned ref) from the `omarchy-vm` ISO-installed template; the layer's baked checks must pass on arch/omarchy (they fail without the mapping — measured exists=false on the pod spike).

**Command (calver 2026.245.1337, /tmp/pi-spike-layer-wf):**

```
/tmp/charly-linux-amd64 check run check-spike-layer-wf-vm
```

**Result — 9/9 steps OK, EXIT=0 (full R10 sequence: build, create, deploy, live, update, fresh-rebuild live):**

```
charly check run check-spike-layer-wf-vm: [vm-build] PASS after 1.747s
charly check run check-spike-layer-wf-vm: [vm-create] PASS after 3.286s
charly check run check-spike-layer-wf-vm: [deploy-add] PASS after 30.42s
charly check run check-spike-layer-wf-vm: [bring-up-members] PASS after 0s
charly check run check-spike-layer-wf-vm: [check-live] PASS after 5.414s
charly check run check-spike-layer-wf-vm: [update] PASS after 4m52.804s
charly check run check-spike-layer-wf-vm: [check-live-rebuild] PASS after 2.882s
charly check run check-spike-layer-wf-vm: [cleanup] PASS after 3m2.13s
charly check run check-spike-layer-wf-vm: [cleanup-members] PASS after 0s
charly check run check-spike-layer-wf-vm: PASS (steps=9)
EXIT=0
```

**The three layer plan checks on the fresh rebuild (check-live-rebuild.log, verbatim):**

```
VM: charly-check-spike-layer-wf-vm (ssh user@127.0.0.1:45933)
  PASS  check the layer's wf-recorder binary lands at /usr/bin/wf-recorder in the guest  [layer-wf-binary]  ok
  PASS  check wf-recorder is registered with pacman from the extra repo  [layer-wf-package]  exit=0
  PASS  check wf-recorder runs (version probe)  [layer-wf-version]  exit=0
```

Summary.yml: 9/9 steps `ok: true`, bed verdict `ok: true`. This is the layer's R10 fresh-rebuild proof (previously deferred to the recording-lane rollout — now landed).

## Rulebook compliance

- **R0 skills:** git-workflow + strict-policy loaded; layer/wf-recorder skill + candy plan consulted (package-map and distro semantics).
- **R1 RCA + zero warnings:** no failures/warnings. The gap was root-caused live: on the omarchy spikes the layer installed nothing and the baked checks failed (wf-recorder binary exists=false) — the missing arch/omarchy mapping, fixed here.
- **R2 no out-of-scope:** one package-map extension (fedora -> fedora + arch + omarchy) + one skill doc line; nothing else touched.
- **R3 no duplication:** one canonical package map, extended; no second mechanism introduced.
- **R4 no workaround:** the mapping is the correct mechanism — matches the distro package reality (pacman -Si wf-recorder -> Repository: extra).
- **R5 hard cutover + grep:** N/A — additive mapping, no identifiers removed; no stale references remain (the doc line now states RPM + PAC).
- **R6 git safety:** status clean before branch/commit; no destructive ops; no force-push.
- **R7 runtime gate:** fresh-rebuild disposable omarchy VM (check-spike-layer-wf-vm, disposable: true) — the gate executed above with pasted output (9/9, EXIT=0).
- **R8 artifact invariants:** N/A — config-only change, no artifacts.
- **R9 binary == source + deps:** N/A — no binary shipped.
- **R10 disposable + coverage:** the changed path (arch/omarchy package install of wf-recorder) EXECUTED on the fresh rebuild — check-live AND check-live-rebuild both PASS all three layer checks (binary, package, version) verbatim above; base provenance: omarchy-vm ISO 4.0.1 unattended install, layer seeded at the pinned ref snapshot.
- **RDD / ADE / SDD:** ADE intact — the layer keeps description + plan + checks, now covering the added distros; SDD N/A.
- **Hard cutover:** one atomic commit; additive only.
- **Kernel/plugin boundary law:** N/A — layer package config, no kernel/plugin surface.
- **Disposable-only autonomy:** additions only; nothing destroyed, no autonomy change.
- **Clean architecture + Go gates:** N/A — no Go.
- **CHANGELOG:** N/A — tag-on-merge writes it from this PR body.

## Change classification

- **Change class:** layer package-map fix (fedora-only -> fedora + arch + omarchy)
- **Verification gate:** fresh-rebuild disposable omarchy VM composing the changed layer — 9/9 steps PASS, EXIT=0 (calver 2026.245.1337, pasted above)
- **Attribution tier:** analysed on a live system

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*